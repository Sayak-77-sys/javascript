//what's the difference between innerHTML and textContent and innerText?

//innerHTML is used to get the HTML content of an element
//textContent is used to get the text content of an element
//innerText is used to get the text content of an element


let div=document.querySelector("div");
console.log(div.innerHTML);
console.log(div.textContent);
console.log(div.innerText);

